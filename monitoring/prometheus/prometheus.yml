global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "app"
    static_configs:
      - targets:
          - app:5000
    metrics_path: /metrics

  - job_name: "processor"
    static_configs:
      - targets:
          - processor:8000
    metrics_path: /metrics